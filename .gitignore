# Local runtime artifacts
*.log
.DS_Store

# ZCode local state (never commit auth/secrets)
.zcode/
.codex/

# Editor
.vscode/
.idea/

# Spike scratch (never in repo)
codex-router-zcode-spike/

# vendored submodule is tracked via .gitmodules, not ignored
